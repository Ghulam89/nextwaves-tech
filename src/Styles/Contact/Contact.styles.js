
const styles = {
    Container: {
        py: "5em",
        position: "relative",
        backgroundImage: theme => `${'linear-gradient(to right, red, #FFFFFF4A)'}, url("${'/avatars/contact.png'}")`
    },
};

export default styles;